# CV done in React by means of Claude Code
## Manual Step in local
### Generation of the PDF file inside `public` directory
The `cv.pdf` is generated locally by running `generate-pdf.sh` *executable*
### Deploy to Github
`npx vite build && npx gh-pages -d dist`


